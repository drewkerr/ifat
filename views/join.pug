extends index.pug

block scripts

block content
  if error
    .message.red #{error}
  #login.page
    form(action="/join" method="get")
      label Enter class code:
        input#code(name="code" maxlength="4" autocapitalize="off" autocorrect="off" autocomplete="off" spellcheck="false")
      input#name(name="name" placeholder="Enter names")
      button Start